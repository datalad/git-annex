[[!comment format=mdwn
 username="joey"
 subject="""comment 5"""
 date="2015-06-03T17:18:35Z"
 content="""
This bug is closed because it was fixed. At least for uploads, which is
what this bug is about. 

Make sure that the remote server and local client both have a recent
version of git-annex, and report a new bug if you have a problem.
"""]]
