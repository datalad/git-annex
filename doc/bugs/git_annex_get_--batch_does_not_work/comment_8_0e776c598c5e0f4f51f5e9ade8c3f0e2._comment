[[!comment format=mdwn
 username="joey"
 subject="""comment 8"""
 date="2022-03-10T17:09:26Z"
 content="""
Other features like --json when used with --batch would
not work well if it recursed through directories. There would be no way to
know which output corresponded with which input.
"""]]
