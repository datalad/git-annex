[[!comment format=mdwn
 username="Lukey"
 avatar="http://cdn.libravatar.org/avatar/c7c08e2efd29c692cc017c4a4ca3406b"
 subject="comment 3"
 date="2020-11-08T08:25:36Z"
 content="""
Hmm, when testing the standalone build, did you actually point `PATH` to it, so that it overrides your host git and git-annex? I.e. `export PATH=\"<path to git-annex.linux>:$PATH\"` and then `which git-annex` should show you the path to the standalone git-annex.
"""]]
