[[!comment format=mdwn
 username="Atemu"
 avatar="http://cdn.libravatar.org/avatar/d1f0f4275931c552403f4c6707bead7a"
 subject="comment 3"
 date="2022-08-28T17:56:20Z"
 content="""
There are still errors but those shouldn't prevent git-annex from working.

Could you run `git annex sync` manually?

Is the name of the gitlab remote really `origin`? Because it still seems to be trying to sync with it. Could you run `git remote -v`?
"""]]
