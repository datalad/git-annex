[[!comment format=mdwn
 username="joey"
 subject="""comment 3"""
 date="2015-07-06T16:25:01Z"
 content="""
Could you adjust the patch to print out the `GIT_SSH` variable?
I'm pretty sure that variable is set so git can see it; let's see if
git-annex can see it. If it cannot, this would hint that it's having some
trouble seeing environment variables in general..
"""]]
