[[!comment format=mdwn
 username="joey"
 subject="""comment 8"""
 date="2020-03-09T18:21:14Z"
 content="""
I think the problem demonstrated by the script is the same problem as
[[bugs/Guard_against_previously_annexed_dotfiles_being_converted_to_git_files__63__]],
which I've now fixed.

(git-annex does propagate -c settings on to all git commands it runs)
"""]]
