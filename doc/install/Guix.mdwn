git-annex can be installed with the [GNU Guix](https://www.gnu.org/software/guix) package manager.

    guix package -i git-annex

Currently (as of March 2021), some features are not enabled in the package.

    -Android -Webapp
