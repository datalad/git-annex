[[!comment format=mdwn
 username="joey"
 subject="""comment 3"""
 date="2017-12-13T16:52:22Z"
 content="""
Of course, `git annex uninit` should make it easy to undo an accidental
init if you know you don't want to use git-annex in the repo.

So, this idea must be more about repos whose users don't know there's a
policy reason not to use git-annex in them.
"""]]
