[[!comment format=mdwn
 username="yarikoptic"
 avatar="http://cdn.libravatar.org/avatar/f11e9c84cb18d26a1748c33b48c924b4"
 subject="comment 4"
 date="2022-02-21T21:53:03Z"
 content="""
just it is getting a bit \"confusing\" to use `.gitattributes` for some types of annotations (e.g., `largefiles`) for files patterns and then `git annex config` for others.  Ideally there should either be a singular or equivalently expressive (not complimentary) multiple ways.

> without the problems of using git attributes

could you please remind (or point to prior composed list) of those? I personally just keep forgetting which one (first or last match) applies ;)
"""]]
