[[!comment format=mdwn
 username="joey"
 subject="""comment 1"""
 date="2017-02-07T16:34:57Z"
 content="""
enableremote can already enable a remote by uuid instead of by name.

It might be useful to let a uuid be provided to initremote. There's also
some foot shooting potential (reuse the wrong uuid and then the special
remote has been set up, so how would you change it).
"""]]
