[[!comment format=mdwn
 username="joey"
 subject="""comment 1"""
 date="2019-09-16T17:24:35Z"
 content="""
You can unset annex.uuid and then re-run `git annex init`.
Will work ok.

You would probably also want to run `git annex fsck --fast`
so that any annexed content in the repo gets recorded as being present in
the new uuid.
"""]]
