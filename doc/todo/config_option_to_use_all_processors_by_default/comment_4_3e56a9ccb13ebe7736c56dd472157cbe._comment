[[!comment format=mdwn
 username="joey"
 subject="""comment 4"""
 date="2018-10-08T16:22:31Z"
 content="""
Ilya, my previous comment was talking about the problem with dedicating -J0
to that.

And yes, parallelization helps with IO, so why run 64 processes on a 64 CPU
system if it has the same speed bus / same network speed as a 4 CPU system?
"""]]
