[[!comment format=mdwn
 username="joey"
 subject="""comment 1"""
 date="2015-10-26T19:29:12Z"
 content="""
git-annex has no better way to find files that linked to that key than the
given log -S.

However, your script can be improved by reading the list of unused keys
from `.git/annex/unused`
"""]]
