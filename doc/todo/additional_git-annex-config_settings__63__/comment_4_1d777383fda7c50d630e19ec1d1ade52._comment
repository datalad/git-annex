[[!comment format=mdwn
 username="joey"
 subject="""comment 4"""
 date="2021-07-12T14:49:12Z"
 content="""
annex.supportunlocked is documented to be about avoiding
extra work needed to support unlocked files, and not
about preventing them from being added to a branch.

In general, I do not want to add global config settings for every single
git config setting. So if your only rartionalle for adding global configs
is that other global configs that are somewhat related exist, that is not a
compelling reason to me.
"""]]
