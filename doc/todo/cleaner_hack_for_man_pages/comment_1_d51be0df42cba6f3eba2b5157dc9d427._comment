[[!comment format=mdwn
 username="joey"
 subject="""comment 1"""
 date="2016-04-11T15:35:43Z"
 content="""
Comparing the outputs, the pandoc man page uses `\f[C]` for
`backticked literals`. At least in my terminal, that does not render
specially at all, unlike the `\fB` that mdwn2man uses to render them in
bold. This makes some man pages significantly harder to follow.
"""]]
